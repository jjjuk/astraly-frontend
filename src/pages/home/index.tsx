import {Text, VStack} from '@chakra-ui/react';
import React from 'react';

interface Props {}

const Home = (props: Props) => {
  return <VStack spacing={{base: '40px', md: '60px'}} my={{base: '30px', md: '60px'}} />;
};

export default Home;
