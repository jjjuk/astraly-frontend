import React from 'react';
import {Text, VStack} from '@chakra-ui/react';

interface Props {}

const Home = (props: Props) => {
  return <VStack spacing={{base: '40px', md: '60px'}} my={{base: '30px', md: '60px'}}></VStack>;
};

export default Home;
